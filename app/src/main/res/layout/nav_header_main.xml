<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="@dimen/height_part_of_avatar"
                android:background="@drawable/side_nav_bar">

    <!--We use this tab to design a circle image for avatar-->
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/image_avatar"
        android:layout_width="@dimen/layout_width"
        android:layout_height="@dimen/layout_height"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginLeft="@dimen/margin_left_avatar"
        android:layout_marginStart="@dimen/margin_start"
        android:src="@android:drawable/sym_def_app_icon"
        app:border_color="@color/color_red"/>

    <!--Username-->
    <TextView
        android:id="@+id/username"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/image_avatar"
        android:layout_alignStart="@+id/image_avatar"
        android:layout_below="@+id/image_avatar"
        android:gravity="left"
        android:paddingBottom="@dimen/padding_bottom"
        android:textStyle="bold"/>

</RelativeLayout>
